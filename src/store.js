// src/store.js
import { writable } from "svelte/store";

// Initialize the store with an empty array
export const countryDataStore = writable([]);

// Other actions or functions related to the store
